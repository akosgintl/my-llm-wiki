@AGENTS.md

<!-- Single source of truth: AGENTS.md at this vault root.
     Claude Code reads CLAUDE.md, Codex reads AGENTS.md; the @-import above makes
     both load the same file, so the schema is never duplicated or drifts.
     Add Claude-Code-specific instructions below this comment, not above it. -->
